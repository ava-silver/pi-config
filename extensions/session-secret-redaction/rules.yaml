rules:
  - name: Datadog API key in DD_API_KEY
    id: custom.datadog-dd-api-key
    pattern: '(?i)DD_API_KEY(?:[^\r\n]{0,100}?)([a-z0-9]{32})'
    confidence: high
    validation:
      type: Assumed
